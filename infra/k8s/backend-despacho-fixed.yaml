apiVersion: v1
kind: Namespace
metadata:
  name: despacho-app
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-despacho
  namespace: despacho-app
  labels:
    app: backend-despacho
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend-despacho
  template:
    metadata:
      labels:
        app: backend-despacho
    spec:
      containers:
        - name: backend-despacho
          image: 548946983683.dkr.ecr.us-east-1.amazonaws.com/backend-despacho:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8081
          env:
            - name: DB_ENDPOINT
              value: mysql
            - name: DB_PORT
              value: "3306"
            - name: DB_NAME
              value: despachos
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: DB_PASSWORD
            - name: JAVA_OPTS
              value: "-XX:+UseG1GC"
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8081
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: backend-despacho
  namespace: despacho-app
spec:
  selector:
    app: backend-despacho
  ports:
    - port: 8081
      targetPort: 8081
  type: ClusterIP
